import ViewWrapper from '../Components/ViewWrapper';

const UserProfile: React.FC = () => {
    return (
        <ViewWrapper>
            <h1>User Profile</h1>
        </ViewWrapper>
    );
}

export default UserProfile;